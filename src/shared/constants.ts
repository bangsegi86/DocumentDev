export const APP_NAME = 'DocumentDev'

/** Id of the inert <script> block that carries the editable JSON payload inside exported HTML. */
export const PAYLOAD_ELEMENT_ID = 'docdev-data'

/** File extension for documents (the exported HTML is also the re-editable project file). */
export const FILE_EXTENSION = 'html'
