// highlight.js color theme, imported as a raw string so it can be inlined into
// the exported self-contained HTML (no CDN/runtime JS needed).
import githubTheme from 'highlight.js/styles/github.css?raw'

export const HIGHLIGHT_CSS = githubTheme
